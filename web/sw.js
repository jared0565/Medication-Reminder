const CACHE='medication-reminder-web-v23';
const ASSETS=['./','./index.html','./styles.css?v=20260723.14','./app.js?v=20260723.14','./update.js?v=20260723.14','./account.js?v=20260723.14','./sync.js?v=20260723.14','./qrcode.js?v=20260723.14','./manifest.webmanifest','./icon.svg'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS))));
self.addEventListener('message',event=>{if(event.data?.type==='SKIP_WAITING')self.skipWaiting()});
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const requestUrl=new URL(event.request.url);const isAppFile=requestUrl.origin===self.location.origin&&['/','/index.html','/app.js','/styles.css','/manifest.webmanifest'].includes(requestUrl.pathname);if(isAppFile){event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));return response}).catch(()=>caches.match(event.request)))}else event.respondWith(caches.match(event.request).then(response=>response||fetch(event.request)))});
self.addEventListener('push',event=>{let data={title:'Medication Reminder',body:'A scheduled reminder is due.'};try{data={...data,...event.data.json()}}catch{}const notification=self.registration.showNotification(data.title,{body:data.body,tag:data.tag||'medication-reminder',data:{url:data.url||'/'}}),notifyClients=data.type==='pair-revoked'?self.clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>list.forEach(client=>client.postMessage({type:'PAIR_REVOKED'}))):Promise.resolve();event.waitUntil(Promise.all([notification,notifyClients]))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{const existing=list.find(client=>new URL(client.url).origin===self.location.origin);return existing?(existing.focus(),existing.navigate(url)):clients.openWindow(url)}))});
